import 'package:flutter/material.dart';

class ChangeViewPage extends StatelessWidget {
  const ChangeViewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Change View'),
    );
  }
} 